[clonotype_merge]
    module       = ClonotypeMerger
    submodule    = ClonotypeMerger
    docker_image = clonotype_merge
    final_output = ig_all_clone_merged, ig_top_clone_merged, t_all_clone_merged, t_top_clone_merged